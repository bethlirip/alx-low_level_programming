#include <stdio.h>

/**
 * main - this is the function of the program
 * description -  we are using puts to print result on the screen
 * Return: Always 0 (Success)
 */
int main(void)
{
	char wo[] = "\"Programming is like building a multilingual puzzle";

	puts(wo);
	return (0);
}
