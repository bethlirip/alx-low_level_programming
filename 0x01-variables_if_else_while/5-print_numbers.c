#include <stdio.h>

/**
 * main - this is the main function
 * Return: this will return 0
 **/
int main(void)
{
	int sd;

	for (sd = 0; sd < 10; sd++)
		printf('%i', sd);

	printf('\n');
	return (0);
}
